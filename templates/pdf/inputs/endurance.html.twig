<div class="row g-0 mx-0 font-weight-bold">
    <div class="col-1 p-0 border border-dark">{{data.date ?? "Brak"}}</div>
    <div class="col-2 p-0 border border-dark">{{data.excercise ?? "Brak"}}</div>
    <div class="col-2 p-0 border border-dark">{{data.intensity ?? "Brak"}}</div>
    <div class="col-1 p-0 border border-dark">{{data.length ?? "Brak"}}</div>
    <div class="col-1 p-0 border border-dark">{{data.series ?? "Brak"}}</div>
    <div class="col-1 p-0 border border-dark">{{data.repetition ?? "Brak"}}</div>
    <div class="col-2 p-0 border border-dark">{{data.work_rest ?? "Brak"}}</div>
    <div class="col-2 p-0 border border-dark">{{data.break ?? "Brak"}}</div>
</div>