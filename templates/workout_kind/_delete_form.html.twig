<form method="post" action="{{ path('workout_kind_delete', {'id': workout_kind.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workout_kind.id) }}">
    <button class="btn">Delete</button>
</form>
