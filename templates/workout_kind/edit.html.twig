{% extends 'base.html.twig' %}

{% block title %}Edit WorkoutKind{% endblock %}

{% block body %}
    <div class="px-5 py-3 container-lg bg-white rounded-lg shadow">
        <div class="d-flex flex-row justify-content-between align-items-end mb-3">
            <h2>Edytuj rodzaj treningu: {{workout_kind.name}}</h2>
             <a href="{{ path('workout_kind_index') }}">
                <button class="btn btn-primary">Powrót do listy</button>
             </a>
        </div>
        {{ include('workout_kind/_form.html.twig', {'button_label': 'Update'}) }}

    </div>
{% endblock %}
