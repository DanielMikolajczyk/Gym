{% extends 'base.html.twig' %}

{% block title %}Excercise{% endblock %}

{% block body %}
    <div class="px-5 py-3 container-lg bg-white rounded-lg shadow">
        <div class="d-flex flex-row justify-content-between align-items-end mb-3">
            <h2>Ćwiczenie: {{excercise.name}}</h2>
            {{ include('excercise/_delete_form.html.twig') }}
             <a href="{{ path('excercise_index') }}">
                <button class="btn btn-primary">Powrót do listy</button>
             </a>
        </div>
        
        <div class="my-3">
            <div class="my-2">
                <h4>Dane użytkownika:</h4>
            </div>
            <div class="ml-3 my-2">
                <span class="font-weight-bold">ID: </span>{{excercise.id}}
            </div>
            <div class="ml-3 my-2">
                <span class="font-weight-bold">Nazwa: </span>{{excercise.name}}
            </div>
            <div class="ml-3 my-2">
                <span class="font-weight-bold">Link url: </span>{{excercise.url}}
            </div>
        </div>        

    </div>

{% endblock %}
