<form method="post" action="{{ path('excercise_delete', {'id': excercise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ excercise.id) }}">
    <button class="btn btn-danger">
        Usuń <i class="fa-solid fa-x"></i>
    </button>
</form>
